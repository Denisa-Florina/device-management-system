services:
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: ${SA_PASSWORD}
      MSSQL_DB: ${DB_NAME}
    ports:
      - "1433:1433"
    volumes:
      - sqldata:/var/lib/mssql/data

volumes:
  sqldata:
